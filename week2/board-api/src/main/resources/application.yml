spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ktb_board?serverTimezone=UTC&useSSL=true&allowPublicKeyRetrieval=true
    username: root
    password: 030928
    driver-class-name: com.mysql.cj.jdbc.Driver

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 20MB

#  jpa:
#    hibernate:
#      ddl-auto: create   # 개발 중에는 create / create-drop / update 중 선택
#    properties:
#      hibernate:
#        format_sql: true
#
#logging:
#  level:
#    org.hibernate.SQL: debug

server:
  port: 1234

file:
  upload-dir: /Users/ktb_ksg/IdeaProjects/KTB_projects/week2/ImageStorage