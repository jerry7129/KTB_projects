apiVersion: apps/v1
kind: Deployment

metadata:
  name: {{ include "ktb-board-spring.deploymentName" . | quote }}
  labels:
    {{- include "ktb-board-spring.labels" . | nindent 4 }}

spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  
  strategy:
    type: {{ .Values.deployment.strategy.type }}
    rollingUpdate:
      maxSurge: {{ .Values.deployment.strategy.rollingUpdate.maxSurge }}
      maxUnavailable: {{ .Values.deployment.strategy.rollingUpdate.maxUnavailable }}

  selector:
    matchLabels:
      {{- include "ktb-board-spring.selectorLabels" . | nindent 6 }}

  template:
    metadata:
      labels:
        {{- include "ktb-board-spring.selectorLabels" . | nindent 8 }}

    spec:
      serviceAccountName: {{ include "ktb-board-spring.serviceAccountName" . | quote }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automount }}

      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      containers:
        - name: {{ .Values.container.name }}

          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}

          ports:
            - name: {{ .Values.container.portName }}
              containerPort: {{ .Values.container.port }}
              protocol: TCP
            - name: {{ .Values.container.managementPortName }}
              containerPort: {{ .Values.container.managementPort }}
              protocol: TCP

          {{- if .Values.serviceAccount.webIdentity.enabled }}
          env:
            - name: AWS_ROLE_ARN
              value: {{ required "serviceAccount.webIdentity.roleArn 값을 지정해야 합니다." .Values.serviceAccount.webIdentity.roleArn | quote }}
            - name: AWS_WEB_IDENTITY_TOKEN_FILE
              value: {{ .Values.serviceAccount.webIdentity.tokenPath | quote }}
            - name: AWS_ROLE_SESSION_NAME
              value: ktb-board-spring

          volumeMounts:
            - name: aws-web-identity-token
              mountPath: /var/run/secrets/aws
              readOnly: true
          {{- end }}

          envFrom:
            {{- if .Values.config.create }}
            - configMapRef:
                name: {{ include "ktb-board-spring.fullname" . }}-config
            {{- end }}

            - secretRef:
                name: {{ required "secret.existingSecret 값을 지정해야 합니다." .Values.secret.existingSecret }}

          {{- if .Values.probes.startup.enabled }}
          startupProbe:
            httpGet:
              path: {{ .Values.probes.startup.httpGet.path | quote }}
              port: {{ .Values.probes.startup.httpGet.port }}
            periodSeconds: {{ .Values.probes.startup.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.startup.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.startup.failureThreshold }}
            successThreshold: {{ .Values.probes.startup.successThreshold }}
          {{- end }}

          {{- if .Values.probes.liveness.enabled }}
          livenessProbe:
            tcpSocket:
              port: {{ .Values.probes.liveness.tcpSocket.port }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
            successThreshold: {{ .Values.probes.liveness.successThreshold }}
          {{- end }}

          {{- if .Values.probes.readiness.enabled }}
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.httpGet.path | quote }}
              port: {{ .Values.probes.readiness.httpGet.port }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
            successThreshold: {{ .Values.probes.readiness.successThreshold }}
          {{- end }}

          resources:
            {{- toYaml .Values.resources | nindent 12 }}

      {{- if .Values.serviceAccount.webIdentity.enabled }}
      volumes:
        - name: aws-web-identity-token
          projected:
            sources:
              - serviceAccountToken:
                  audience: {{ .Values.serviceAccount.webIdentity.audience | quote }}
                  expirationSeconds: {{ .Values.serviceAccount.webIdentity.expirationSeconds }}
                  path: token
      {{- end }}
